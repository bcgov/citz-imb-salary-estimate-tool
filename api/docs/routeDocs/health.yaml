### PATHS ###
paths:
  /health:
    get:
      tags:
        - Health
      summary: Checks if the API is running and healthy.
      description: Checks if the API is running and healthy.
      responses:
        '200':
          description: OK
          content: 
            text/plain:
              schema:
                type: string
                example: 'Salary Estimate Tool API is healthy and ready'
