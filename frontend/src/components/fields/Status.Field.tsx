import { FieldProps } from './FieldProps.d';

export const StatusField = (props: FieldProps) => {
  const { value } = props;
  return <div>TODO: StatusField: {value as string}</div>;
};

export default StatusField;
