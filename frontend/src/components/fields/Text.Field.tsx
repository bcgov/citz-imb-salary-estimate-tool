import { FieldProps } from './FieldProps.d';

export const TextField = (props: FieldProps) => {
  const { value } = props;
  return <div>TODO: TextField: {value as string}</div>;
};

export default TextField;
