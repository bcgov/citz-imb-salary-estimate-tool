import { Box, Typography } from '@mui/material';

export const HMInquiry = () => {
  return (
    <Box p={2}>
      <Typography variant="h4">HM Inquiry</Typography>
    </Box>
  );
};

export default HMInquiry;
